<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="16dp"
    android:background="@color/material_dynamic_primary99">

    <!-- Title TextView with enhanced styling -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to PhoneShop"
        android:textSize="24sp"
        android:textColor="@color/black"
        android:layout_marginBottom="24dp"
        android:fontFamily="sans-serif-medium"
        android:textAlignment="center"/>

    <!-- ImageView with a placeholder image and rounded corners -->
    <ImageView
        android:id="@+id/ivImage"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginVertical="16dp"
        android:scaleType="centerCrop"
        android:src="@drawable/error_placeholder"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="Shop image" />

    <!-- Get Started Button with rounded corners and modern color -->
    <Button
        android:id="@+id/loginButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Get Started"
        android:backgroundTint="@color/material_dynamic_primary0"
        android:textColor="@android:color/white"
        android:paddingVertical="12dp"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium" />

</LinearLayout>
